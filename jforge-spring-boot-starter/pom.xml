<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.erdsgfc</groupId>
        <artifactId>jforge</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>jforge-spring-boot-starter</artifactId>
    <name>jforge-spring-boot-starter</name>
    <description>Spring Boot starter that wires the ORM's programmatic transactions to
        Spring's PlatformTransactionManager, replacing the built-in thread-local
        SimpleTransactionManager at startup.</description>

    <properties>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM: manages spring-tx/spring-jdbc/spring-boot-autoconfigure
                 and the test-stack versions for this module. Only this module sees it. -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>3.5.6</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- The ORM core whose TransactionManager SPI this starter replaces. -->
        <dependency>
            <groupId>io.github.erdsgfc</groupId>
            <artifactId>jforge-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Spring Boot auto-configuration support (consumers are Spring Boot apps). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <!-- PlatformTransactionManager / DataSourceTransactionManager / DataSourceUtils. -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-jdbc</artifactId>
        </dependency>

        <!-- Test stack. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>io.github.erdsgfc</groupId>
                            <artifactId>jforge-processor</artifactId>
                            <version>${project.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <annotationProcessors>
                        <annotationProcessor>io.github.erdsgfc.jforge.processor.EntityProcessor</annotationProcessor>
                        <annotationProcessor>io.github.erdsgfc.jforge.processor.RepositoryProcessor</annotationProcessor>
                    </annotationProcessors>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>