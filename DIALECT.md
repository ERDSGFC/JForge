# JForge 数据库方言（Dialect）架构

编译期代码生成、运行时零反射的 ORM，方言决策同样遵循这条主线：**方言是编译期决策，生成"死的" SQL——运行时没有任何方言对象、零动态分发**。

## 1. 为什么需要方言

SQL 有标准，但每个数据库都在标准之外有实现差异。JDBC 已经吸收了一部分差异（见第 4 节），剩下的——**DDL 类型、SQL 语法片段、生成键的优化路径**——需要方言来覆盖。

JForge 目前只支持两个方言（`Dialect` 枚举，jforge-annotation）：

```java
public enum Dialect {
    POSTGRESQL,   // PostgreSQL / H2（MODE=PostgreSQL）
    MYSQL,        // MySQL / MariaDB
    SQLITE        // SQLite（3.35+）
}
```

H2 测试库走 `POSTGRESQL` 方言（`MODE=PostgreSQL` 下支持 `BIGSERIAL`/`RETURNING`）。

## 2. 核心原则：编译期决策，而非运行时分发

| | MyBatis / Hibernate | JForge |
|---|---|---|
| 方言形态 | **运行时对象**（`dialect.getLimitSql()` 动态分发） | **处理器内部能力表**，编译期选好 |
| 生成 SQL | 运行时拼装 | 编译期拼进 SQL 常量/代码 |
| 运行时成本 | 方言对象 + 动态分发 | **零**（生成的代码就是手写 JDBC） |

```
用户配置 @JForgeConfig(dialect = MYSQL) 或 @JForgeConfig(dialectClass = MyDialect.class)
        ↓ 编译期
处理器加载方言实现 → 按方言选择生成形态 → 生成"死的" SQL 常量 / 绑定代码
        ↓ 运行时
无方言对象、无动态分发、无反射
```

- `Dialect` 枚举（内建 PG/MySQL 的配置值）与 `DialectSupport` 接口（用户扩展点）→ jforge-annotation
- 内建实现（`PostgreSqlDialect`/`MySqlDialect`）与加载逻辑 → 只存在于处理器，是编译期实现细节

## 3. 数据库差异全景（按维度）

### 3.1 标识符：引用符与大小写折叠

**引用符**（给标识符加引号时的写法）：

| 数据库 | 引用符 | 备注 |
|---|---|---|
| PostgreSQL / H2 / Oracle / SQLite | `"user_entity"` | SQL 标准 |
| MySQL | `` `user_entity` `` | 双引号默认是**字符串字面量**，需 `ANSI_QUOTES` 模式 |
| SQL Server | `[user_entity]` | 或双引号（`QUOTED_IDENTIFIER ON`） |

**大小写折叠**（未加引号的标识符被统一大小写）：PostgreSQL 折叠为**小写**；H2 / MySQL / Oracle 折叠为**大写**。加引号 = 精确名字，不折叠。

**对 JForge 的含义**：生成 SQL 一律**不加引号** + 表名/列名约定**全小写 snake_case**——折叠规则差异完全规避（全小写在任何折叠方向下自洽）。代价：**保留字**（如 `order`、`user` 作列名）无法使用——未来若支持保留字，引用符差异就由方言提供（PG/H2 双引号、MySQL 反引号）。

### 3.2 自增/生成主键的 DDL 类型

| 数据库 | 自增列写法 |
|---|---|
| PostgreSQL | `BIGSERIAL`（或 `IDENTITY`，PG 10+） |
| H2（PG 模式） | `BIGSERIAL` |
| MySQL / MariaDB | `BIGINT AUTO_INCREMENT` |
| SQL Server | `BIGINT IDENTITY(1,1)` |
| Oracle | `NUMBER GENERATED BY DEFAULT AS IDENTITY`（12c+） |
| SQLite | `INTEGER PRIMARY KEY AUTOINCREMENT` |

**影响**：未来 DDL 生成（`CREATE TABLE`）时，自增列类型必须按方言生成——这是**差异最大的维度**。

### 3.3 生成键回写（save 的性能与正确性）

| 方式 | 数据库 | 说明 |
|---|---|---|
| `Statement.RETURN_GENERATED_KEYS` | JDBC 标准，PG/H2/MySQL 都支持 | 通用路径（JForge 当前实现） |
| `INSERT ... RETURNING id` | PostgreSQL / H2 | **单语句拿 id**，PG 官方推荐，比 getGeneratedKeys 更优 |
| `getGeneratedKeys` 批量限制 | MySQL Connector/J 旧版 | **只返回批量中最后一条的键**——批量回写需要驱动支持（H2/PG 支持） |

**影响**：save 的生成键回写路径。方言差异点：PG/H2 可走 `RETURNING` 优化路径，MySQL 必须走 JDBC 标准路径。**这是现在就有性能收益的方言项**（框架定位"与手写 JDBC 等效"——手写 PG 代码就会用 RETURNING）。

### 3.4 分页

| 数据库 | 分页写法 |
|---|---|
| PostgreSQL / MySQL / H2 / SQLite | `LIMIT ? OFFSET ?` |
| SQL Server（2012+）/ Oracle（12c+） | `OFFSET ? ROWS FETCH NEXT ? ROWS ONLY` |
| SQL Server（旧版） | `SELECT TOP ? ...` |
| Oracle（旧版） | `ROWNUM <= ?`（嵌套子查询） |

**影响**：分页查询生成。JForge 当前只支持 PG/MySQL 两个方言——两者分页写法相同，此维度暂不产生差异；扩展方言（SQL Server/Oracle）时是主要差异点。

### 3.5 UPSERT（saveOrUpdate）

| 数据库 | 写法 |
|---|---|
| PostgreSQL | `INSERT ... ON CONFLICT (col) DO UPDATE SET ...` |
| MySQL / MariaDB | `INSERT ... ON DUPLICATE KEY UPDATE col = VALUES(col)` |
| SQL Server / Oracle | `MERGE INTO ... USING ... WHEN MATCHED THEN UPDATE ...` |
| SQLite | `INSERT OR REPLACE INTO ...` |

**影响**：未来 `saveOrUpdate` 特性的生成。

### 3.6 布尔与字面量

| 数据库 | 布尔 |
|---|---|
| PostgreSQL / H2 | `TRUE` / `FALSE` 字面量，`BOOLEAN` 类型 |
| MySQL | `1` / `0`，`TINYINT(1)` 模拟 |
| SQL Server | `BIT`，字面量 `1`/`0` |

**影响**：DDL 生成（BOOLEAN vs TINYINT(1)）与默认值/查询字面量。

### 3.7 日期时间

| 数据库 | 当前时间 |
|---|---|
| SQL 标准 / H2 | `CURRENT_TIMESTAMP` |
| PostgreSQL | `CURRENT_TIMESTAMP` / `NOW()` / `CLOCK_TIMESTAMP()` |
| MySQL | `CURRENT_TIMESTAMP` / `NOW()` |
| Oracle | `SYSDATE` / `SYSTIMESTAMP` |
| SQL Server | `GETDATE()` / `SYSUTCDATETIME()` |

**影响**：默认值表达式（DDL）与框架生成的时间相关 SQL。另注意 TIMESTAMP 精度差异（PG/H2 微秒、MySQL 默认秒/可配微秒）。

### 3.8 字符串函数

| 操作 | PostgreSQL | MySQL |
|---|---|---|
| 拼接 | `\|\|` | `CONCAT(a, b)`（`\|\|` 是 OR） |
| 长度 | `LENGTH(s)`（字符数） | `LENGTH(s)`（字节数）/ `CHAR_LENGTH(s)` |
| 转义 | `ESCAPE '\'` 标准 | 反斜杠默认转义（NO_BACKSLASH_ESCAPES 可关） |

**影响**：**@Query 是用户手写 SQL——字符串函数差异由用户负责**；框架生成代码（CRUD）不涉及这些函数。方言不需要覆盖。

### 3.9 锁与隔离

| 特性 | 支持 |
|---|---|
| `SELECT ... FOR UPDATE` | 通用 |
| `FOR UPDATE SKIP LOCKED` | PG 9.5+ / MySQL 8.0+ |
| `NOWAIT` | PG / Oracle |

**影响**：未来悲观锁查询生成。

### 3.10 NULL 排序

| 数据库 | 行为 |
|---|---|
| PostgreSQL | `NULLS FIRST` / `NULLS LAST` 显式控制 |
| SQL Server | `NULLS FIRST/LAST`（2008+） |
| MySQL | **无**——NULL 恒排最前（升序） |

**影响**：排序查询生成（需要方言决定是否/如何表达 NULL 排序）。

### 3.11 类型映射（DDL 生成核心）

| 语义 | PostgreSQL | MySQL |
|---|---|---|
| 自增主键 | `BIGSERIAL` | `BIGINT AUTO_INCREMENT` |
| 布尔 | `BOOLEAN` | `TINYINT(1)` |
| 字符串 | `VARCHAR(n)` | `VARCHAR(n)` |
| 时间戳 | `TIMESTAMP(6)` | `TIMESTAMP(6)`（可配精度） |
| 大文本 | `TEXT` | `LONGTEXT` |

**影响**：DDL 生成（schema 管理）时按方言映射实体类型 → 列类型。这是方言能力表里最重的部分。

### 3.12 其他差异

- **大小写敏感性**：MySQL 表名大小写随文件系统（Linux 区分、Windows 不区分）——全小写 snake 规避
- **批量插入**：`INSERT INTO t VALUES (...),(...),(...)` 多行写法 PG/MySQL 都支持；驱动批量 `addBatch` 的生成键支持差异（见 3.3）
- **全文检索 / 正则**：各库语法差异大——只影响用户手写 SQL，不由框架生成

## 4. JDBC 标准吸收的部分（不需要方言）

以下差异已被 JDBC 统一，**方言不需要覆盖**：

| 能力 | JDBC 吸收方式 |
|---|---|
| 占位符 | 一律 `?`（`PreparedStatement`） |
| 生成键 | `getGeneratedKeys()` 标准 API（各驱动实现差异，方言只在"是否更优路径"层面介入） |
| 批量 | `addBatch()/executeBatch()` 标准 API |
| 事务 | `setAutoCommit/commit/rollback` 标准 API |
| 连接管理 | `DataSource` / 连接池（HikariCP） |
| 元数据 | `DatabaseMetaData`（驱动差异大，但框架不依赖具体值） |

**结论**：框架生成的**执行机制**全部走 JDBC 标准——方言只决定"生成什么 SQL"，不决定"怎么执行"。

## 5. 方言 SPI 设计（接口 + `dialectClass` 配置）

### 5.1 接口（jforge-annotation，用户可见的扩展点）

```java
package io.github.erdsgfc.jforge.annotation;

/**
 * 数据库方言 SPI：处理器在编译期加载实现，把方言差异直接拼进生成的 SQL。
 * 框架内置 POSTGRESQL/MYSQL 两个实现；其他数据库由用户实现本接口，
 * 通过 @JForgeConfig(dialectClass = ...) 指定。
 *
 * <p>重要：方言在编译期使用——实现类必须位于编译 classpath（独立模块预编译
 * 或先前已编译），不能与主源码同批编译（注解处理期间同批类尚无 class 文件）。</p>
 */
public interface DialectSupport {

    /** 生成键回写：RETURNING（单语句拿 id）或 JDBC 标准 getGeneratedKeys。 */
    boolean supportsReturningKeys();

    /** 自增列的 DDL 类型（未来 schema 生成用）。 */
    String serialType();

    /** 标识符引用符（保留字列名/表名支持用）。 */
    String quote();

    /** 分页片段（含占位符）。 */
    String limitClause();

    /** UPSERT 子句（未来 saveOrUpdate 用）。 */
    String upsertClause();

    /** 布尔字面量。 */
    String booleanLiteral();
}
```

### 5.2 配置与加载（无 ServiceLoader——用户显式给类名）

```java
// jforge-annotation：@JForgeConfig 增加 dialectClass（String = 方言实现的全限定名）
String dialectClass() default "";

// 用户用法
@JForgeConfig(dialectClass = "com.example.MyOracleDialect")  // 自己的实现（预编译进 classpath）
@JForgeConfig(dialect = Dialect.MYSQL)                       // 内建（dialectClass 缺省时）
```

```java
// 处理器加载（JForgeConfigHelper.dialectSupport）——Class.forName + 无参构造：
DialectSupport support = config.dialectClass().isEmpty()
        ? builtIn(config.dialect())                    // 内建 PG/MySQL
        : (DialectSupport) Class.forName(config.dialectClass())
                .getDeclaredConstructor().newInstance();
```

**为什么用 String 而非 `Class` 属性**：javac 注解处理器读取 `Class` 类型属性时，**即使取默认值也会尝试加载类并抛 `MirroredTypeException`**（实测）——String + `Class.forName` 彻底绕开，且加载失败（类名写错/未实现接口/无无参构造/未预编译）时错误消息明确。

**为什么不用 ServiceLoader**：JForge 的场景是"内建 2 个 + 用户显式加 1-2 个"——配置类名比"写 `META-INF/services` 注册文件 + 按名匹配"简单得多。ServiceLoader 是"框架自动发现用户生态"（几十个方言、插件式扩展）的量级才需要的。

### 5.3 内建实现（jforge-processor 内部）

```java
final class PostgreSqlDialect implements DialectSupport {
    public boolean supportsReturningKeys() { return false; }  // 暂未启用,见下
    public String serialType()   { return "BIGSERIAL"; }
    public String quote()        { return "\""; }
    public String limitClause()  { return "LIMIT ? OFFSET ?"; }
    public String upsertClause() { return "ON CONFLICT"; }
    public String booleanLiteral() { return "TRUE"; }
}
// MySqlDialect：supportsReturningKeys=false、BIGINT AUTO_INCREMENT、`、LIMIT ? OFFSET ?、
// ON DUPLICATE KEY、1
// SqliteDialect：supportsReturningKeys=true（3.35+，官方推荐 RETURNING）、
// INTEGER PRIMARY KEY AUTOINCREMENT、"、LIMIT ? OFFSET ?、ON CONFLICT、1
```

> **PostgreSqlDialect 的 `supportsReturningKeys` 当前为 false 的原因**：`INSERT ... RETURNING` 是真 PG 的优化路径，但 H2 2.3（测试库，同样走 `POSTGRESQL` 方言）**实测不支持该语法**（三种形式均语法错误）——方言无法区分两者，统一走 JDBC 标准 `getGeneratedKeys`。待引入独立 H2 方言或运行时驱动探测后再启用（接口与生成分支已就位，改返回值即生效）。
>
> **SqliteDialect 的 `supportsReturningKeys` 为 true**：SQLite 3.35+（2021）支持 `INSERT ... RETURNING`，xerial JDBC 驱动内置引擎均为新版本——RETURNING 是官方推荐的生成键方式（`getGeneratedKeys` 在 SQLite 上等价于 `last_insert_rowid()`）。旧版本引擎需降级。

### 5.4 生成形态示例（save 生成键回写——方言就位后的两种形态）

```java
// PostgreSqlDialect（supportsReturningKeys=true 时）→ RETURNING（单语句拿 id）
try (PreparedStatement ps = conn.prepareStatement("INSERT INTO users (...) VALUES (...) RETURNING id")) {
    try (ResultSet rs = ps.executeQuery()) {
        rs.next();
        entity.id(rs.getLong(1));
    }
}

// 当前实际形态（两内建方言均走 JDBC 标准）→ getGeneratedKeys
try (PreparedStatement ps = conn.prepareStatement("INSERT INTO users (...) VALUES (...)",
        Statement.RETURN_GENERATED_KEYS)) {
    ps.executeUpdate();
    try (ResultSet keys = ps.getGeneratedKeys()) { /* 回写 */ }
}
```

### 5.5 设计规则

1. **方言选择一次完成**：`SqlCodegen`/`SqlFieldGenerator` 接收 `DialectSupport`，生成点只查 `support.xxx`——不散落 `if (dialect == X)`
2. **降级策略**：方言只影响"生成什么"；JDBC 标准路径永远是兜底（如驱动不支持 `RETURNING` 时降级 `getGeneratedKeys`）
3. **H2 走 POSTGRESQL 方言**：`MODE=PostgreSQL` 下 `BIGSERIAL`/`RETURNING` 均支持——测试即方言验证
4. **处理器内实例化不违反"运行时零反射"**：`Class.forName`/`newInstance` 是编译期工具逻辑；生成的代码不含任何方言对象——运行时依旧零方言、零分发
5. **不做运行时方言**：方言对象/分发不进框架 jar——那是 MyBatis 的形态，与 JForge 定位冲突

## 6. 演进路线

| 阶段 | 方言项 | 状态 |
|---|---|---|
| 现在 | `Dialect` 枚举 + `configHelper.dialect()` | ✅ 已存在（未接线） |
| 近期 | `DialectSupport` 接口 + `dialectClass` 配置 + 内建两实现 + save 的 `RETURNING` 优化路径 | 待落地（PG 下即有性能收益） |
| 中期 | DDL 生成（`serialType`/类型映射/`booleanLiteral`） | 需要方言最重的部分 |
| 远期 | UPSERT（`upsertClause`）、分页（`limitClause`）、锁/NULL 排序 | 特性驱动扩展 |

**优先级判断**：当前框架的 CRUD 全部走 JDBC 标准能力，方言差异实际很小；方言架构的价值在于**提前定好"差异在哪里、怎么表达"**，让 DDL/UPSERT/分页等特性加入时直接扩展接口方法，而不是临时在生成点塞 `if`。

## 7. 参考：主流 ORM 的方言设计

- **Hibernate**：`org.hibernate.dialect.Dialect` 运行时抽象类（每个数据库一个子类），提供 `getAddColumnString()`、`getLimitHandler()` 等数百个钩子——最重量级
- **MyBatis**：运行时 `Dialect` 接口（仅分页等少数能力），配合 `@SelectProvider` 手写 SQL——轻量
- **JForge 定位**：介于两者之间但形态不同——**编译期能力表**，只覆盖"生成 SQL 时需要知道的差异"，其余全部交给 JDBC 标准
